{% extends 'base.html.twig' %}

{% block title %}My Profile - Mentionable{% endblock %}

{% block body %}
    <div class="main-panel">
        <div class="content-wrapper p-0">
            <div class="row justify-content-center px-0 mb-4 mt-5">
                <div class="col-12 w-100 p-3" style="background-color: #181b20; border-radius: 5px;">
                    {% if not user.isBanned %}
                        {% for label, messages in app.flashes %}
                            {% set bootstrap_class = label == 'error' ? 'danger' : label %}
                            {% for message in messages %}
                                <div class="alert alert-{{ bootstrap_class }} alert-dismissible fade show mt-3" role="alert">
                                    {{ message }}
                                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                                </div>
                            {% endfor %}
                        {% endfor %}
                    {% endif %}

                    <div class="row flex-column flex-md-row g-4 bg-dark rounded ps-4 pe-4 pb-4 shadow" style="margin-top: -7px">
                        <div class="col-md-4 text-center text-md-start">
                            {% if user.avatar %}
                                <div class="avatar-pic">
                                    <div class="avatar-indicator position-relative d-inline-block">
                                        <img src="{{ asset('uploads/avatars/' ~ user.avatar) }}"
                                             alt="Avatar"
                                             class="img-thumbnail img-fluid rounded-circle mt-3"
                                             style="width: 400px; height: auto;"
                                             onerror="showDefaultAvatar(this)"/>
                                    </div>
                                </div>
                            {% endif %}
                        </div>

                        <div class="col-md-6">
                            <div class="d-flex flex-column flex-md-row mb-2 align-items-start align-items-md-center">
                                <div class="d-flex flex-row m-0 p-0">
                                    <h2 class="mt-4 text-white"><strong>@</strong>{{ user.nametag }}</h2>
                                    {% if user.isVerified %}
                                        <i class='count fa fa-check-circle ms-2' style="margin-top: 30px; font-size: 28px"></i>
                                    {% endif %}
                                </div>
                                    {% if app.user.id is not same as(user.id) %}
                                        {% if app.user not in user.friends %}
                                            <form action="{{ path('app_profile_add_friend', {'id': user.id}) }}" method="post">
                                                <input type="hidden" name="friend_id" value="{{ user.id }}" />
                                                <button type="submit" class="btn btn-sm btn-light ms-0 ms-md-4 mt-1 mt-md-3" style="width: 75px; height: 30px;">
                                                    Add
                                                </button>
                                            </form>
                                        {% else %}
                                            <form action="{{ path('app_profile_remove_friend', {'id': user.id}) }}" method="post" class="row g-2">
                                                <input type="hidden" name="friend_id" value="{{ user.id }}" />
                                                <button type="submit" class="btn btn-sm btn-outline-danger ms-0 ms-md-4 mt-1 mt-md-3" style="height: 30px; border: 1px solid #dc3545;">
                                                    Unfriend
                                                </button>
                                            </form>
                                        {% endif %}
                                    {% endif %}
                            </div>
                            <a href="{{ path('app_profile_view_friends', {'id': user.id}) }}" id="friends-list" class="text-white" style="text-decoration: none;">
                                <p class="mt-3" style="font-size: 15px; font-weight: bold">
                                    {{ user.friends|length }}
                                    {% if user.friends|length == 1 %}
                                        friend
                                    {% else %}
                                        friends
                                    {% endif %}
                                </p>
                            </a>

                            <h4 class="text-gray mt-3">Profile Information</h4>

                            <p>joined on {{ user.creationDate|date('d.m.Y') }}</p>

                            {# INTERESTS #}
                            {% if app.user.id is same as(user.id) %}
                                    <h4 class="text-gray mt-3">Interests</h4>
                                    <div class="d-flex flex-wrap gap-2 m-0 p-1 rounded">
                                        {% for interest in interests %}
                                            <div class="bg-light px-2 py-1 text-dark rounded-pill" style="user-select: none">
                                                <a href="{{ path('app_interest_show', { id: interest.id }) }}" class="text-dark" style="text-decoration: none;">
                                                    #{{ interest }}
                                                </a>
                                                <a href="#" class="delete-interest text-dark ms-2" data-interest-id="{{ interest.id }}" style="text-decoration: none; font-weight: bold;">x</a>
                                            </div>
                                        {% endfor %}
                                        <a class="bg-white px-2 py-1 text-dark rounded-pill" style="text-decoration: none;" href="#"
                                           data-bs-toggle="modal"
                                           data-bs-target="#addInterestModal">
                                            <b class="ms-1 me-1">+</b>
                                        </a>
                                        <div class="modal fade" id="addInterestModal" tabindex="-1" aria-labelledby="addInterestModalLabel" aria-hidden="true" style="overflow-y: hidden;">
                                            <div class="modal-dialog">
                                                <div class="modal-content border-success">
                                                    <div class="modal-header bg-success text-white">
                                                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                                                    </div>
                                                    <div class="modal-body">
                                                        <p class="m-0 p-0">Add an interest for your profile so people can find out what you like.</p>
                                                        <p class="mb-4 mt-0 pt-0">Note: An interest tag cannot contain blank spaces.</p>
                                                        <form method="POST" enctype="multipart/form-data" action="{{ path('app_interest_new') }}" id="addInterestForm">
                                                            <textarea name="interest-content" id="interest-content" placeholder="Write..." class="form-control mb-4" style="height: 45px;" required></textarea>
                                                            <div class="d-flex justify-content-end">
                                                                <button type="button" class="btn btn-light me-2" data-bs-dismiss="modal" aria-label="Close">Close</button>
                                                                <button type="submit" class="btn btn-success" style="width: 120px;">Add</button>
                                                            </div>
                                                        </form>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                            {% else %}
                                {% if interests is not empty %}
                                    <h4 class="text-gray mt-3">Interests</h4>
                                    <div class="d-flex flex-wrap gap-2 m-0 p-1 rounded">
                                        {% for interest in interests %}
                                            <div class="bg-light px-2 py-1 text-dark rounded-pill" style="user-select: none">
                                                <a href="{{ path('app_interest_show', { id: interest.id }) }}" class="text-dark" style="text-decoration: none;">
                                                    #{{ interest }}
                                                </a>
                                            </div>
                                        {% endfor %}
                                    </div>
                                {% endif %}
                            {% endif %}

                            <h4 class="text-gray mt-3">Bio</h4>

                            <div class="d-flex flex-row">
                                <p class="me-2">{{ bio|slice(0, 255) }}</p>
                                {% if app.user.id is same as(user.id) %}
                                    <a class="bg-white px-2 text-dark rounded-pill pt-1 pb-0" style="text-decoration: none; height: 28px" href="#"
                                       data-bs-toggle="modal"
                                       data-bs-target="#editProfileBioModal">
                                        <i class='bx bx-edit-alt text-dark' style="font-size: 18px"></i>
                                    </a>
                                    <div class="modal fade" id="editProfileBioModal" tabindex="-1" aria-labelledby="editProfileBioModalLabel" aria-hidden="true" style="overflow-y: hidden;">
                                        <div class="modal-dialog">
                                            <div class="modal-content border-light">
                                                <div class="modal-header bg-dark" style="height: 50px">
                                                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                                                </div>
                                                <div class="modal-body">
                                                    <p class="mb-4 p-0">Want to make a change? Edit your bio here:</p>
                                                    <form class="mb-3" method="POST" enctype="multipart/form-data" action="{{ path('app_bio_new') }}" id="addProfileBioForm">
                                                        <textarea name="bio-content" id="bio-content" placeholder="Write..." class="form-control mb-4" style="height: 90px;" required>{{ bio }}</textarea>
                                                        <div class="d-flex justify-content-end">
                                                            <button type="submit" class="btn btn-light" style="width: 120px;">Edit</button>
                                                        </div>
                                                    </form>
                                                    <i class="p-0 text-danger" style="font-size: 14px">Note: The content cannot be longer than 255 characters.</i>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                {% endif %}
                            </div>


                            {# RINGS - MOBILE DISPLAY ONLY #}
                            <div class="d-block d-md-none m-0 p-0">
                                <h4 class="text-gray mt-3">Rings</h4>
                                {% if ringsMobileDisplay|length %}
                                    {% if ringsMobileDisplay|length < 5 %}
                                        <div class="d-flex flex-wrap gap-2 m-0 p-1 rounded">
                                            {% for ring in ringsMobileDisplay %}
                                                <div class="bg-dark px-2 py-1 text-light rounded-pill" style="user-select: none; border: 1px solid white;">
                                                    <a href="{{ path('app_ring_show', { id: ring.id }) }}" class="text-light" style="text-decoration: none;">
                                                        {{ ring.title }}
                                                    </a>
                                                </div>
                                            {% endfor %}
                                        </div>
                                    {% else %}
                                        <div class="d-flex flex-wrap gap-2 m-0 p-1 rounded">
                                            {% for ring in ringsMobileDisplay %}
                                                <div class="bg-dark px-2 py-1 text-light rounded-pill" style="user-select: none; border: 1px solid white;">
                                                    <a href="{{ path('app_ring_show', { id: ring.id }) }}" class="text-light" style="text-decoration: none;">
                                                        {{ ring.title }}
                                                    </a>
                                                </div>
                                            {% endfor %}
                                        </div>
                                        <a href="#" class="text-white"
                                           data-bs-toggle="modal"
                                           data-bs-target="#seeAllRingsModal">
                                            <p class="mt-2 ms-2">See all</p>
                                        </a>
                                        <div class="modal fade" id="seeAllRingsModal" tabindex="-1" aria-labelledby="seeAllRingsModalLabel" aria-hidden="true" style="overflow-y: hidden;">
                                            <div class="modal-dialog">
                                                <div class="modal-content border-light">
                                                    <div class="modal-header bg-dark text-white">
                                                        <h5 class="modal-title" id="deleteNoteModalLabel">{{ user.nametag }}'s rings</h5>
                                                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                                                    </div>
                                                    <div class="modal-body" style="max-height: 60vh; overflow-y: auto;">
                                                        {% if rings|length %}
                                                            {% for ring in rings %}
                                                                <div class="d-flex align-items-center rounded p-2">
                                                                    <a href="{{ path('app_ring_show', {'id': ring.id}) }}">
                                                                        <img src="{{ asset('uploads/rings/' ~ ring.banner) }}"
                                                                             class="rounded-circle me-3 border border-light"
                                                                             alt=""
                                                                             style="width: 48px; height: 48px; object-fit: cover;">
                                                                    </a>
                                                                    <div class="flex-grow-1" style="max-width: calc(100% - 120px);">
                                                                        <a href="{{ path('app_ring_show', {'id': ring.id}) }}" style="text-decoration: none;">
                                                                            <h6 class="mb-0 text-light" title="{{ ring.title }}">{{ ring.title }}</h6>
                                                                        </a>
                                                                        <a href="{{ path('app_interest_show', {'id': ring.interest.id}) }}"
                                                                           class="ms-0 align-self-start text-decoration-none"
                                                                           style="line-height: 1; padding: 0;">
                                                                            <span class="bg-light px-2 py-1 text-dark rounded-pill d-inline-block mt-1"
                                                                                  style="font-size: 13px; line-height: 1;">
                                                                                    #{{ ring.interest.title }}
                                                                            </span>
                                                                        </a>
                                                                    </div>

                                                                    <a href="{{ path('app_ring_show', {'id': ring.id}) }}"
                                                                       class="btn btn-outline-light btn-sm" style="font-weight: bold">
                                                                        Visit
                                                                    </a>
                                                                </div>
                                                            {% endfor %}
                                                        {% endif %}
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    {% endif %}
                                {% endif %}
                            </div>
                        </div>
                        {% if user.isBanned %}
                            {% for label, messages in app.flashes %}
                                {% set bootstrap_class = label == 'error' ? 'danger' : label %}
                                {% for message in messages %}
                                    <div class="alert alert-{{ bootstrap_class }} alert-dismissible fade show mt-3" role="alert">
                                        {{ message|raw }}
                                    </div>
                                {% endfor %}
                            {% endfor %}
                        {% endif %}
                    </div>

                    {% if user.id == app.user.id %}
                        <div class="mt-4 bg-dark rounded p-4 shadow text-white">
                            <h4 class="mb-3">Add a Friend</h4>
                            <form action="{{ path('app_profile_add_friend', {'id': user.id}) }}" method="post" class="row g-2">
                                <div class="col-md-9">
                                    <input type="text" name="friend_nametag" placeholder="Enter friend's nametag" class="form-control">
                                </div>
                                <div class="col-md-3 d-grid">
                                    <button type="submit" class="btn btn-success">Send Request</button>
                                </div>
                            </form>
                        </div>

                        <div class="mt-4 bg-dark rounded p-4 shadow text-white">
                            <h4 class="mb-3">Friend Requests Received</h4>
                            {% if friendRequests is not empty %}
                                <ul class="list-group">
                                    {% for request in friendRequests %}
                                        <li class="list-group-item bg-secondary text-dark fw-bold d-flex justify-content-between align-items-center">
                                            {{ request.sender.nametag }}
                                            <div>
                                                <a href="{{ path('app_profile_accept_friend', {'id': request.id}) }}" class="btn btn-sm btn-success me-2">Accept</a>
                                                <a href="{{ path('app_profile_reject_friend', {'id': request.id}) }}" class="btn btn-sm btn-danger">Decline</a>
                                            </div>
                                        </li>
                                    {% endfor %}
                                </ul>
                            {% else %}
                                <p class="text-muted">No friend requests received.</p>
                            {% endif %}
                        </div>
                    {% else %}
                        {% if user.isBanned == 0 %}
                            <div class="row g-4 ps-1 pe-1 pb-4 shadow" style="margin-top: -7px">
                            <div class="col-md-8 col-12">
                                <h4 class="mt-5 text-center text-md-center text-light">Notes</h4>
                                {# NOTES LIST #}
                                {% if notesWithMentionedUser is defined and notesWithMentionedUser|length > 0 %}
                                    {% for item in notesWithMentionedUser %}
                                            {# NOTE #}
                                            <div class="bg-dark rounded p-4 shadow mb-3 pt-4 ps-4 pe-4 pb-3" style="background-color: #181b20; border-radius: 5px; margin-top: 2rem;">
                                                {# NOTE PROFILE #}
                                                <div class="navbar-profile d-flex flex-row text-light p-2 ps-3 mb-3" style="width: 100%;">
                                                    <a class="nav-link me-3 mt-2" id="avatarNote" href="{{ path('app_profile', {'id': item.note.user.id}) }}">
                                                        <img class="img-sm rounded-circle" src="{{ asset('uploads/avatars/' ~ item.note.user.avatar) }}" alt="avatar" onerror="showDefaultIcon(this)">
                                                    </a>
                                                    <div class="d-flex flex-column">
                                                        <a class="nav-link mt-3" id="nametagNote" href="{{ path('app_profile', {'id': item.note.user.id}) }}">
                                                            <h5 class="mb-0 navbar-profile-name">@{{ item.note.user.nametag }}</h5>
                                                        </a>
                                                        <div class="d-flex flex-column m-0 p-0" style="font-size: 12px; color: gray">
                                                            <div class="d-flex flex-row m-0 p-0">
                                                                {% if item.note.isEdited %}
                                                                    <i class="fa fa-clock-o mt-1 me-1"></i>
                                                                {% endif %}
                                                                <p>{{ item.note.humanTimePost }}</p>
                                                                <p class="ms-1 me-1">•</p>
                                                                <a class="m-0 p-0" style="text-decoration: none; color: #d1d0d0" href="{{ path('app_note_show', {'noteId': item.note.id}) }}">
                                                                    <p>View</p>
                                                                </a>
                                                            </div>
                                                            {% if item.note.ring %}
                                                                <p class="me-1 mb-1" style="margin-top: -17px">from
                                                                    <a href="{{ path('app_ring_show', {'id': item.note.ring.id}) }}" style="text-decoration: none; color: white">
                                                                        {{ item.note.ring.title }}
                                                                    </a>
                                                                </p>
                                                                {% set key = item.note.user.id ~ '_' ~ item.note.ring.id %}
                                                                {% set role = roles[key]|default('Member') %}
                                                                <div class="d-flex align-items-center" style="gap: 3px; font-size: 13px;">
                                                                    <p class="mb-0 text-muted" style="white-space: nowrap;">Role:</p>
                                                                    {% set bgColor = role == 'member' ? '#6c757d' : 'orange' %}
                                                                    <span style="
                                                                        background-color: {{ bgColor }};
                                                                        color: white;
                                                                        padding: 2px 8px;
                                                                        border-radius: 12px;
                                                                        font-weight: 600;
                                                                        text-transform: capitalize;
                                                                        white-space: nowrap;">
                                                                        {{ role|capitalize }}
                                                                    </span>
                                                                </div>
                                                            {% endif %}
                                                        </div>
                                                    </div>
                                                    {% if item.note.user.id != 24 or (item.note.user.id == 24 and app.user.id == 24) %}
                                                        <div class="dropdown ms-auto mt-1 me-1">
                                                            <a href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false" style="text-decoration: none;">
                                                                <i class="fa fa-sort-desc" style="color: white"></i>
                                                            </a>
                                                            <ul class="dropdown-menu">
                                                                {% if app.user.nametag == item.note.user.nametag %}
                                                                    <li>
                                                                        <a class="dropdown-item" href="{{ path('app_note_update', {id: item.note.id}) }}">Edit...</a>
                                                                    </li>
                                                                    <li>
                                                                        <a class="dropdown-item" href="#"
                                                                           data-bs-toggle="modal"
                                                                           data-bs-target="#deleteNoteModal"
                                                                           data-note-id="{{ item.note.id }}"
                                                                           data-note-token="{{ csrf_token('delete' ~ item.note.id) }}">
                                                                            Delete note
                                                                        </a>
                                                                    </li>
                                                                {% elseif item.note.user.id != 24 and app.user.nametag != item.note.user.nametag %}
                                                                    <li>
                                                                        <a class="dropdown-item" href="{{ path('app_note_report', {id: item.note.id}) }}">Report</a>
                                                                    </li>
                                                                {% endif %}
                                                            </ul>
                                                            {# FAVORITE BUTTON #}
                                                            <button class="favorite-btn btn btn-sm ms-1
                                                                    {% if favoritesMap[item.note.id] %}
                                                                        btn-warning
                                                                    {% else %}
                                                                        btn-outline-warning
                                                                    {% endif %}"
                                                                    data-id="{{ item.note.id }}"
                                                                    style="border-radius: 50%">
                                                                <i class="bx {% if favoritesMap[item.note.id] %}bxs-star{% else %}bx-star{% endif %}" style="font-size: 18px; margin-top: 5px;"></i>
                                                            </button>
                                                            <div class="modal fade" id="deleteNoteModal" tabindex="-1" aria-labelledby="deleteNoteModalLabel" aria-hidden="true" style="overflow-y: hidden;">
                                                                <div class="modal-dialog">
                                                                    <div class="modal-content border-danger">
                                                                        <div class="modal-header bg-danger text-white">
                                                                            <h5 class="modal-title" id="deleteNoteModalLabel">Confirm note deletion</h5>
                                                                            <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                                                                        </div>
                                                                        <div class="modal-body">
                                                                            Are you sure you want to delete this note?
                                                                        </div>
                                                                        <div class="modal-footer">
                                                                            <form method="post" id="deleteNoteForm" action="">
                                                                                <input type="hidden" name="_token" id="deleteTokenInput" value="">
                                                                                <button type="button" class="btn btn-light me-2" data-bs-dismiss="modal">Cancel</button>
                                                                                <button type="submit" class="btn btn-danger">Delete</button>
                                                                            </form>
                                                                        </div>
                                                                    </div>
                                                                </div>
                                                            </div>
                                                        </div>
                                                    {% else %}
                                                        <div class="dropdown ms-auto mt-1 me-1">
                                                            {# FAVORITE BUTTON #}
                                                            <button class="favorite-btn btn btn-sm ms-1
                                                                    {% if favoritesMap[item.note.id] %}
                                                                        btn-warning
                                                                    {% else %}
                                                                        btn-outline-warning
                                                                    {% endif %}"
                                                                    data-id="{{ item.note.id }}"
                                                                    style="border-radius: 50%">
                                                                <i class="bx {% if favoritesMap[item.note.id] %}bxs-star{% else %}bx-star{% endif %}" style="font-size: 18px; margin-top: 5px;"></i>
                                                            </button>
                                                        </div>
                                                    {% endif %}
                                                </div>
                                                {# NOTE CONTENT #}
                                                <div class="d-flex flex-row text-white ps-3 mb-1">
                                                    <p style="font-size: 16px; white-space: pre-wrap;">{{ item.note.content }}</p>
                                                </div>
                                                {# NOTE IMAGE #}
                                                {% if item.note.image %}
                                                    <div class="d-flex flex-row text-gray justify-content-center mb-2">
                                                        <a href="{{ path('app_note_show', {'noteId': item.note.id}) }}">
                                                            <img
                                                                {% if item.note.image starts with 'https://' %}
                                                                    src="{{ item.note.image }}"
                                                                {% else %}
                                                                    src="{{ asset('uploads/notes/' ~ item.note.image)}}"
                                                                {% endif %}
                                                                class="img-fluid" id="noteImage" alt="noteImage" onerror="showDefaultImage(this)"
                                                            />
                                                        </a>
                                                    </div>
                                                {% endif %}
                                                {# NOTE NAV – DESKTOP & MOBILE HYBRID #}
                                                <div class="d-flex flex-wrap align-items-center text-gray mt-2 mb-1 ms-0 ps-0">
                                                    {# VOTE & COMMENTS – ALWAYS SHOWN #}
                                                    <div class="d-flex flex-row flex-wrap">
                                                        {# UPVOTE #}
                                                        <button class="upvote-btn btn btn-sm d-flex align-items-center border-0
                                                            {% if votesMap[item.note.id] is defined and votesMap[item.note.id] == 'upvote' %}
                                                                btn-light
                                                            {% else %}
                                                                btn-outline-light
                                                            {% endif %}"
                                                            data-id="{{ item.note.id }}">
                                                            <i class='bx bx-arrow-up' style="font-size: 20px;"></i>
                                                            <span class="ms-1" id="upvotes-{{ item.note.id }}">{{ item.note.getUpVote() }}</span>
                                                        </button>

                                                        {# DOWNVOTE #}
                                                        <button class="downvote-btn btn btn-sm d-flex align-items-center border-0
                                                            {% if votesMap[item.note.id] is defined and votesMap[item.note.id] == 'downvote' %}
                                                                btn-light
                                                            {% else %}
                                                                btn-outline-light
                                                            {% endif %}"
                                                            data-id="{{ item.note.id }}">
                                                            <i class='bx bx-arrow-down' style="font-size: 20px;"></i>
                                                            <span class="ms-1" id="downvotes-{{ item.note.id }}">{{ item.note.getDownVote() }}</span>
                                                        </button>

                                                        {# COMMENTS BUTTON #}
                                                        <button type="button"
                                                                class="btn btn-outline-light btn-sm d-flex flex-row align-items-center border-0 ps-2 pe-2 ms-1 toggle-comments compact-btn"
                                                                data-note-id="{{ item.note.id }}">
                                                            <i class='bx bx-message-circle-reply pe-1'></i>
                                                            <span class="comment-count">{{ item.note.comments|length }}</span>
                                                        </button>

                                                        <div class="ms-2 me-2 pt-1 d-none d-md-block">
                                                            <p class="text-white" style="margin-top: 7px">•</p>
                                                        </div>

                                                        {# NOTE MENTIONED – SHOWN ONLY ON DESKTOP #}
                                                        <div class="d-none d-md-block ps-1 pe-1" style="margin-top: 10px">
                                                            <p class="mb-0 mt-0 text-white" style="font-size: 16px">
                                                                mentioned
                                                                {% if item.mentionedUser %}
                                                                    <a class="m-0 p-0 text-white" href="{{ path('app_profile', {'id': item.mentionedUser.id}) }}" style="text-decoration: none;">
                                                                        <b style="background-color: #2A3038">@{{ item.mentionedUser.nametag }}</b>
                                                                    </a>
                                                                {% endif %}
                                                            </p>
                                                        </div>
                                                    </div>

                                                    {# NOTE MENTIONED – SHOWN ONLY ON MOBILE, BELOW BUTTONS #}
                                                    <div class="d-block d-md-none w-100 mt-2 ps-3 pe-1 mb-2">
                                                        <p class="text-white" style="margin-bottom: -2px; font-size: 16px">
                                                            mentioned
                                                            {% if item.mentionedUser %}
                                                                <a class="m-0 p-0 text-white" href="{{ path('app_profile', {'id': item.mentionedUser.id}) }}" style="text-decoration: none;">
                                                                    <b style="background-color: #2A3038">@{{ item.mentionedUser.nametag }}</b>
                                                                </a>
                                                            {% endif %}
                                                        </p>
                                                    </div>
                                                </div>

                                                {# NOTE COMMENT #}
                                                <div class="note-comments" id="comments-{{ item.note.id }}" style="display: none;">
                                                    <form method="POST" action="{{ path('note_comment', {id: item.note.id}) }}" class="ajax-comment-form d-flex flex-column mb-3" style="position: relative" data-note-id="{{ item.note.id }}">
                                                        <div class="d-flex flex-row align-items-center">
                                                            <img class="img-xs rounded-circle comment-avatar me-2" src="{{ asset('uploads/avatars/' ~ app.user.avatar)}}" alt="writeCommentAvatar" onerror="showDefaultIcon(this)">
                                                            <div class="d-flex flex-row w-100">
                                                                <input type="text" name="message" class="form-control comment-input mb-1" placeholder="Write a comment..."
                                                                       style="border-bottom-right-radius: 0; border-top-right-radius: 0;" required>
                                                                <div class="d-none d-sm-flex justify-content-end position-relative">
                                                                    <button type="button" class="btn btn-light btn-sm px-3 emoji-toggle-btn"
                                                                            style="height: 2.575rem; border-bottom-left-radius: 0; border-top-left-radius: 0; background-color: #252c35;">
                                                                        <i class='bx bx-happy-beaming' style='color:#ffffff; font-size: 24px'></i>
                                                                    </button>
                                                                </div>
                                                                <div class="gif-container me-1 d-flex position-relative">
                                                                    <button type="button" class="btn btn-light btn-sm text-white gif-toggle-btn"
                                                                            style="height: 2.575rem; font-size: 20px; border-bottom-left-radius: 0; border-top-left-radius: 0; background-color: #2A3038;font-weight: bold"
                                                                            data-bs-toggle="modal"
                                                                            data-bs-target="#gifCommentModal"
                                                                            data-note-id="{{ item.note.id }}">
                                                                        GIF
                                                                    </button>
                                                                </div>
                                                                <div class="d-flex justify-content-end">
                                                                    <button type="submit" class="btn btn-light btn-sm px-3" style="height: 40px;">Post</button>
                                                                </div>
                                                            </div>
                                                        </div>
                                                        <div class="alert alert-danger alert-dismissible fade show comment-error d-none mt-2" role="alert">
                                                            <span class="error-message"></span>
                                                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                                                        </div>
                                                        <div class="emoji-list position-absolute p-2 rounded" style="display:none; background-color: #2A3038; bottom: 100%; right: 0; max-height: 200px; overflow-y: auto; z-index: 1000;"></div>
                                                        {# GIF PREVIEW #}
                                                        <div class="gif-preview-container d-none mt-2 mb-2"
                                                             id="gif-preview-container-{{ item.note.id }}"
                                                             style="position: relative; width: 100%;">
                                                            <div class="d-flex flex-column align-items-center">
                                                                <div class="gif-wrapper position-relative d-inline-block">
                                                                    <button type="button"
                                                                            class="btn-close btn-close-white gif-preview-clear position-absolute top-0 end-0"></button>
                                                                    <img class="gif-preview img-fluid"
                                                                         id="gif-preview-{{ item.note.id }}"
                                                                         src=""
                                                                         alt="Selected GIF"
                                                                         style="border: 1px solid #fff; height: auto; max-height: 300px; width: auto; display: block;">
                                                                    <input type="hidden" name="gif_url" class="gif-url-input" id="gif-url-{{ item.note.id }}">
                                                                    <p class="bg-light text-dark text-center m-0"
                                                                       style="border-radius: 0 0 0.375rem 0.375rem; width: 100%;">
                                                                        Preview
                                                                    </p>
                                                                </div>
                                                            </div>
                                                        </div>
                                                    </form>
                                                    <div class="new-comments"></div>

                                                    {# NOTE COMMENTS LIST #}
                                                    <div id="comments-list-{{ item.note.id }}">
                                                        {% for comment in limitedComments[item.note.id] %}
                                                            <div class="d-flex flex-row mb-3 comment-item" data-note-id="{{ item.note.id }}">
                                                                <a class="nav-link me-2" href="{{ path('app_profile', {'id': comment.user.id}) }}">
                                                                    <img class="img-xs rounded-circle comment-avatar" src="{{ asset('uploads/avatars/' ~ comment.user.avatar) }}" alt="avatarComment" onerror="showDefaultIcon(this)">
                                                                </a>
                                                                <div class="flex-grow-1">
                                                                    <div class="d-flex flex-row align-items-center">
                                                                        <a class="m-0 p-0 text-decoration-none text-white" href="{{ path('app_profile', {'id': comment.user.id}) }}">
                                                                            <p class="text-white me-2 mb-0" style="font-size: 16px;">@{{ comment.user.nametag }}</p>
                                                                        </a>
                                                                        {% if comment.isEdited %}
                                                                            <i class="fa fa-clock-o text-gray me-1" style="font-size: 14px;"></i>
                                                                        {% else %}
                                                                            <p class="text-gray mb-0 me-1">•</p>
                                                                        {% endif %}
                                                                        <p class="text-gray mb-0" style="font-size: 14px;">{{ comment.humanTimeComment }}</p>
                                                                    </div>
                                                                    <div class="d-inline-flex align-items-center">
                                                                        <div class="d-flex flex-column">
                                                                            {% if comment.message %}
                                                                                <small class="d-inline-block" style="font-size: 16px; align-self: flex-start; max-width: 300px; color: #404040; background-color: #ffffff; border-radius: 8px; opacity: 0.9; padding: 5px;">
                                                                                    {{ comment.message }}
                                                                                </small>
                                                                            {% endif %}
                                                                            {% if comment.image %}
                                                                                <div class="comment-gif-container mb-1" style="width: 100%; max-width: 200px;">
                                                                                    <img src="{{ comment.image }}"
                                                                                         alt="GIF"
                                                                                         style="width: 100%; height: auto; object-fit: contain; border-radius: 8px; display: block;">
                                                                                </div>
                                                                            {% endif %}
                                                                        </div>
                                                                        {# UPVOTE FORM #}
                                                                        <div class="d-flex flex-row ms-2">
                                                                            <button class="btn btn-sm ms-2 d-flex align-items-center border-0 comment-upvote-btn
                                                                                    {% if commentVotesMap is defined and commentVotesMap[comment.id] is defined and commentVotesMap[comment.id] == 'upvote' %}
                                                                                            btn-light
                                                                                    {% else %}
                                                                                        btn-outline-light
                                                                                    {% endif %}"
                                                                                    type="button"
                                                                                    data-note-id="{{ comment.note.id }}"
                                                                                    data-comment-id="{{ comment.id }}"
                                                                                    data-csrf="{{ csrf_token('vote') }}">
                                                                                <i class='bx bx-arrow-up' style="font-size: 20px;"></i>
                                                                                <span id="comment-upvotes-{{ comment.id }}" class="ms-1">{{ comment.upVote ?? 0 }}</span>
                                                                            </button>
                                                                            <button class="btn btn-sm d-flex align-items-center border-0"
                                                                                    type="button"
                                                                                    data-bs-toggle="modal" data-bs-target="#newCommentReplyModal" data-comment-id="{{ comment.id }}"
                                                                                    data-comment-message="{{ comment.message }}"
                                                                                    data-username="{{ comment.user.nametag }}"
                                                                                    data-comment-img="{{ comment.image }}"
                                                                            >
                                                                                <i class='bx bx-message-circle-plus' style='color:#ffffff; font-size: 20px;'></i>
                                                                                <span class="ps-2">{{ comment.commentReplies|length }}</span>
                                                                            </button>
                                                                        </div>
                                                                    </div>
                                                                </div>
                                                                <div class="dropdown ms-auto me-1 d-flex align-items-start">
                                                                    <a href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false" style="height: 30px;">
                                                                        <i class='bx bx-dots-horizontal-rounded' style='color:#ffffff; font-size: 20px;'></i>
                                                                    </a>
                                                                    <ul class="dropdown-menu m-0">
                                                                        {% if app.user.nametag == comment.user.nametag %}
                                                                            <li><a class="dropdown-item" href="{{ path('note_comment_update', {id: comment.id, noteId: item.note.id}) }}">Edit...</a></li>
                                                                            <li><a class="dropdown-item delete-comment-btn" href="{{ path('note_comment_delete', {id: comment.id, noteId: item.note.id}) }}">Delete comment</a></li>
                                                                        {% else %}
                                                                            <li><a class="dropdown-item" href="{{ path('note_comment_report', {id: comment.id, noteId: item.note.id}) }}">Report</a></li>
                                                                        {% endif %}
                                                                    </ul>
                                                                </div>
                                                            </div>
                                                        {% endfor %}
                                                        {% if item.note.comments|length > limitedComments[item.note.id]|length %}
                                                            <div class="text-start mt-2">
                                                                <a href="#" class="see-all-comments text-warning" data-note-id="{{ item.note.id }}">
                                                                    <small>See all comments</small>
                                                                </a>
                                                            </div>
                                                        {% endif %}
                                                    </div>
                                                </div>
                                            </div>
                                        {% endfor %}
                                    {% else %}
                                        <div class="mt-3 bg-dark rounded p-4 shadow pt-4 ps-4 pe-4 pb-3">
                                            <p class="text-light">Oops. No notes available here.</p>
                                        </div>
                                    {% endif %}
                            </div>
                            <div class="col-md-4 d-md-block d-none">
                                <h4 class="mt-5 text-center text-md-center text-light">Rings</h4>
                                {# RINGS LIST - DESKTOP DISPLAY ONLY #}
                                <div class="divScrollBar bg-dark rounded p-3 pt-4 shadow" style="margin-top: 2rem; max-height: 60vh; overflow-y: auto;">
                                    {% if rings|length %}
                                        <div class="d-flex flex-column gap-3">
                                            {% for ring in rings %}
                                                <div class="d-flex align-items-center rounded p-2">
                                                    <a href="{{ path('app_ring_show', {'id': ring.id}) }}">
                                                        <img src="{{ asset('uploads/rings/' ~ ring.banner) }}"
                                                             class="rounded-circle me-3 border border-light"
                                                             alt=""
                                                             style="width: 48px; height: 48px; object-fit: cover;">
                                                    </a>
                                                    <div class="flex-grow-1" style="max-width: calc(100% - 120px);">
                                                        <a href="{{ path('app_ring_show', {'id': ring.id}) }}" style="text-decoration: none;">
                                                            <h6 class="mb-0 text-light" title="{{ ring.title }}">{{ ring.title }}</h6>
                                                        </a>
                                                        <a href="{{ path('app_interest_show', {'id': ring.interest.id}) }}"
                                                           class="ms-0 align-self-start text-decoration-none"
                                                           style="line-height: 1; padding: 0;">
                                                            <span class="bg-light px-2 py-1 text-dark rounded-pill d-inline-block mt-1"
                                                                  style="font-size: 13px; line-height: 1;">
                                                                    #{{ ring.interest.title }}
                                                            </span>
                                                        </a>
                                                    </div>

                                                    <a href="{{ path('app_ring_show', {'id': ring.id}) }}"
                                                       class="btn btn-outline-light btn-sm" style="font-weight: bold">Visit
                                                    </a>
                                                </div>
                                            {% endfor %}
                                        </div>
                                    {% else %}
                                        <p class="text-muted text-center mb-0">No communities joined.</p>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                        {% endif %}
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
    <div class="modal fade" id="gifCommentModal" tabindex="-1" aria-labelledby="gifCommentModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-custom">
            <div class="modal-content" style="background-color: #181b20; color: white; border-radius: 5px;">
                <div class="modal-header">
                    <h5 class="modal-title" id="gifCommentModalLabel">Select a GIF</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <input type="text" class="form-control gif-search mb-2" placeholder="Search GIFs...">
                    <div class="gif-results d-grid gap-2" style="grid-template-columns: repeat(3, 1fr);"></div>
                </div>
            </div>
        </div>
    </div>
    {# COMMENT REPLY MODAL #}
    <div class="modal fade" id="newCommentReplyModal" tabindex="-1" aria-hidden="true" style="overflow-y: hidden;">
        <div class="modal-dialog" style="max-width: 850px; margin-top: 50px">
            <div class="modal-content bg-dark text-light">
                <div class="modal-header">
                    <h5 class="modal-title">Reply to @<span id="reply-username"></span></h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body p-4" style="max-height: 70vh; overflow-y: auto;">
                    <div id="parent-comment-message"
                         class="d-inline-block mb-3"
                         style="font-size: 20px; font-weight: bold; align-self: flex-start; max-width: 300px;
                                color: #404040; background-color: #ffffff; border-radius: 8px;
                                opacity: 0.9; padding: 5px; display: none;">
                    </div>
                    <div style="position: relative; width: 100%;">
                        <img id="parent-comment-img" src="" style="height: auto; max-height: 300px; width: auto;" alt="reply-gif">
                    </div>
                    <form class="ajax-reply-form mt-3" id="reply-form" method="POST" action="">
                        <input type="hidden" name="_token" value="{{ csrf_token('reply') }}">
                        <input type="hidden" name="parent_comment_id" id="parent-comment-id">
                        <div class="d-inline-flex flex-row p-0" style="width: 100%;">
                            <textarea class="form-control" name="message" rows="3" placeholder="Write your reply..."></textarea>
                            <button type="submit" class="btn btn-light">Reply</button>
                        </div>
                    </form>
                    <h3 class="mt-3">Replies</h3>
                    <div class="new-replies mt-3" id="replies-container"></div>
                </div>
            </div>
        </div>
    </div>
    <script src="{{ asset('../assets/js/comment-replies-modal.js') }}"></script>
{% endblock %}

{% block javascript %}
{{ parent() }}
<script>
    window.votesMap = {{ votesMap|json_encode|raw }};
</script>
<script>
    document.addEventListener("DOMContentLoaded", function () {
        const buttons = document.querySelectorAll(".toggle-comments");

        buttons.forEach(button => {
            button.addEventListener("click", function () {
                const noteId = this.getAttribute("data-note-id");
                const commentSection = document.getElementById("comments-" + noteId);

                if (commentSection) {
                    if (commentSection.style.display === "none" || commentSection.style.display === "") {
                        commentSection.style.display = "block";
                    } else {
                        commentSection.style.display = "none";
                    }
                } else {
                    console.error("Nu s-a găsit elementul pentru comentarii cu id: comments-" + noteId);
                }
            });
        });
    });
</script>
<script>
document.addEventListener('click', function (e) {
    if (e.target.classList.contains('delete-interest')) {
        e.preventDefault();

        const interestId = e.target.dataset.interestId;

        fetch(`/profile/interest/delete/${interestId}`, {
            method: 'DELETE',
            headers: {
                'X-Requested-With': 'XMLHttpRequest'
            }
        })
        .then(res => res.json())
        .then(data => {
            if (data.success) {
                e.target.closest('.rounded-pill').remove();
            } else {
                alert(data.message || 'Failed to delete interest');
            }
        })
         .catch(err => {
            console.error('Delete error:', err);
            alert('Error deleting interest: ' + err.message);
        });
    }
});
</script>
<script>
    window.commentVotesMap = {{ commentVotesMap|json_encode|raw }};
    window.currentUserNametag = "{{ app.user ? app.user.nametag : '' }}";
    window.csrfToken = "{{ csrf_token('comment_vote') }}";
</script>
<script>
document.addEventListener('DOMContentLoaded', function () {
    document.querySelectorAll('.see-all-comments').forEach(function(link) {
        link.addEventListener('click', function(e) {
            e.preventDefault();
            const noteId = this.dataset.noteId;
            const commentsContainer = document.getElementById('comments-list-' + noteId);
            const url = '/note/' + noteId + '/comments';

            fetch(url)
                .then(res => res.json())
                .then(data => {
                    const commentsArray = Array.isArray(data) ? data : (data.comments || []);
                    const existingIds = Array.from(commentsContainer.querySelectorAll('[data-comment-id]'))
                        .map(el => el.dataset.commentId);

                    commentsArray.forEach(comment => {
                        if (!comment || !comment.user || !comment.id) return;
                        if (!existingIds.includes(String(comment.id))) {

                            const isUpvoted = window.commentVotesMap && window.commentVotesMap[comment.id] === 'upvote';

                            const messageHtml = comment.message
                                ? `<small class="d-inline-block"
                                          style="font-size: 16px; align-self: flex-start; max-width: 300px; color: #404040;
                                                 background-color: #ffffff; border-radius: 8px; opacity: 0.9; padding: 5px;">
                                        ${comment.message}
                                   </small>`
                                : '';

                            const gifHtml = comment.image
                                ? `<div class="comment-gif-container mb-1" style="width: 100%; max-width: 200px;">
                                        <img src="${comment.image.startsWith('http') ? comment.image : '/uploads/notes/' + comment.image}"
                                             alt="GIF"
                                             style="width: 100%; height: auto; object-fit: contain; border-radius: 8px; display: block;">
                                   </div>`
                                : '';

                            const html = `
                                <div class="d-flex flex-row mb-3" data-note-id="${noteId}" data-comment-id="${comment.id}">
                                    <a class="nav-link me-2" href="/profile/${comment.user.id}/view">
                                        <img class="img-xs rounded-circle"
                                             src="/uploads/avatars/${comment.user.avatar}"
                                             alt="avatarComment"
                                             onerror="showDefaultIcon(this)">
                                    </a>
                                    <div class="ms-2 p-0">
                                        <div class="d-flex flex-row" style="height: 30px;">
                                            <a class="m-0 p-0 text-decoration-none text-white" href="/profile/${comment.user.id}/view">
                                                <p class="text-white me-2" style="font-size: 18px;">@${comment.user.nametag}</p>
                                            </a>
                                            ${comment.isEdited
                                                ? `<i class="fa fa-clock-o text-gray me-1" style="margin-top: 6px"></i>`
                                                : `<p class="text-gray mt-1 me-2">•</p>`
                                            }
                                            <p class="text-gray mt-1">${comment.humanTime}</p>
                                        </div>
                                        <div class="d-inline-flex align-items-center">
                                            <div class="d-flex flex-column">
                                                ${messageHtml}
                                                ${gifHtml}
                                            </div>
                                            <div class="d-flex flex-column ms-2">
                                                <button class="btn btn-sm ms-2 d-flex align-items-center border-0 comment-upvote-btn ${isUpvoted ? 'btn-light' : 'btn-outline-light'}"
                                                        type="button"
                                                        data-note-id="${noteId}"
                                                        data-comment-id="${comment.id}"
                                                        data-csrf="${window.csrfToken}">
                                                    <i class='bx bx-arrow-up' style="font-size: 20px;"></i>
                                                    <span id="comment-upvotes-${comment.id}" class="ms-1">${comment.upVote || 0}</span>
                                                </button>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="dropdown ms-auto me-1 d-inline-flex justify-content-end">
                                        <a href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false" style="height: 30px;">
                                            <i class='bx bx-dots-horizontal-rounded' style='color:#ffffff'></i>
                                        </a>
                                        <ul class="dropdown-menu m-0">
                                            ${comment.user.nametag === window.currentUserNametag
                                                ? `<li><a class="dropdown-item" href="/post/comment/${noteId}-${comment.id}/update">Edit...</a></li>
                                                   <li><a class="dropdown-item delete-comment-btn" href="/post/comment/${noteId}-${comment.id}/delete">Delete comment</a></li>`
                                                : `<li><a class="dropdown-item" href="/note/comment/${noteId}-${comment.id}/report">Report</a></li>`
                                            }
                                        </ul>
                                    </div>
                                </div>
                            `;

                            commentsContainer.insertAdjacentHTML('beforeend', html);
                        }
                    });

                    this.style.display = 'none';
                })
                .catch(err => console.error('Error loading comments:', err));
        });
    });
});
</script>
<script src="{{ asset('../assets/js/feed.js') }}"></script>
<script src="{{ asset('../assets/js/comments.js') }}"></script>
<script src="{{ asset('../assets/js/comment-replies.js') }}"></script>
<script src="{{ asset('../assets/js/comment-replies-modal.js') }}"></script>
<script src="{{ asset('../assets/js/favorite.js') }}"></script>
<script src="{{ asset('../assets/js/emoji.js') }}"></script>
<script src="{{ asset('../assets/js/gif.js') }}"></script>
{% endblock %}
